## Notification App Backend

Fetches notifications from the evaluation API and returns top 10 priority notifications ranked by type weight (Placement > Result > Event) and recency.

Endpoint: GET /notifications/priority